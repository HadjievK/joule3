litellm==1.86.1
langchain==1.3.9
langchain-core==1.4.7
langchain-litellm==0.3.5
langgraph==1.2.5
a2a-sdk==0.3.22
fastapi!=0.136.3
uvicorn==0.40.0
httpx==0.28.1
python-dotenv==1.2.2
click==8.3.3
mcp>=1.0.0
opentelemetry-instrumentation-starlette
sap-cloud-sdk @ file:vendor/sap_cloud_sdk-0.29.1-py3-none-any.whl
# Pin protovalidate to last version without a google-re2 hard dependency.
# protovalidate>=0.14 makes google-re2 a mandatory dep; google-re2 has no musllinux wheel
# so uv's isolated musl Python build environment cannot install it without a C++ compiler.
# Keeping 0.13.0 means google-re2 never enters the resolution graph at all.
protovalidate==0.13.0
PyYAML==6.0.3
pydantic
# Microsoft Agent Governance Toolkit — policy enforcement, OWASP ASI 2026 coverage
agent-governance-toolkit[full]==4.1.0
